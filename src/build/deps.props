<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="Current" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup Label="UserMacros">
    <wxPath Condition="'$(wxPath)' == ''"></wxPath>
    <SDLPath Condition="'$(SDLPath)' == ''"></SDLPath>
    <FFPath Condition="'$(FFPath)' == ''"></FFPath>
    <TFPath Condition="'$(TFPath)' == ''"></TFPath>
    <AviSynthPath Condition="'$(AviSynthPath)' == ''"></AviSynthPath>
  </PropertyGroup>
  <Import Project="$(MSBuildThisFileDirectory)deps.props.user" Condition="exists('$(MSBuildThisFileDirectory)deps.props.user')" />
</Project>
